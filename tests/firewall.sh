#!/bin/sh
# tests/firewall.sh — Firewall zone, interface, forwarding rule tests
#
# Placeholder for firewall-specific tests. Currently firewall functionality
# is covered indirectly by library structure tests (detect_firewall_backend,
# setup_tailscale_interface, etc.). Dedicated tests will be added here as
# firewall logic grows.

run_firewall_tests() {
    :
}
